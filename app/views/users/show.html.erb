<% @user.each do |user| %>
	<%= user["id"] %>
	::
	<%= user["name"] %>
<% end %>


<% @albums.each do |album| %>
	<%= album["title"] %>

	<%= link_to "#{album["title"] }", album_path(album["id"]), class: "text text-gray-700 text-sm" %>
<% end %>
